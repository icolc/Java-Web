<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.xrui.mapper.EmpMapper">

    <!--添加数据-->
    <insert id="insert">
        insert into emp
        values (#{id}, #{username}, #{password}, #{name}, #{gender}, #{image}, #{job}, #{entrydate}, #{deptId},
                #{createTime}, #{updateTime})
    </insert>

    <!--修改员工-->
    <update id="update">
        update emp
        <set>
            <if test="username != null and username.trim() != '' ">
                username = #{username},
            </if>
            <if test="password != null and password.trim() != '' ">
                password = #{password},
            </if>
            <if test="name != null and name.trim() != ''">
                name =#{name},
            </if>
            <if test="gender != null">
                gender = #{gender},
            </if>
            <if test="image !=null and image.trim() != ''">
                image=#{image},
            </if>
            <if test="job !=null">
                job=#{job},
            </if>
            <if test="entrydate !=null ">
                entrydate=#{entrydate},
            </if>
            <if test="deptId !=null">
                dept_id=#{deptId},
            </if>
            <if test="updateTime !=null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--删除数据-->
    <delete id="delete">
        delete from emp
        <where>
            id in
            <foreach collection="ids" item="id" separator="," open="(" close=")">
                #{id}
            </foreach>
        </where>
    </delete>

    <!--根据部门ID删除-->
    <delete id="deleteByDetpId">
        delete
        from emp
        where dept_id = #{id}
    </delete>

    <!--根据条件查-->
    <select id="list" resultType="com.xrui.pojo.Emp">
        select *
        from emp
        <where>
            <if test="name != null and name.trim() != '' ">
                name like concat('%',#{name},'%')
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="begin != null and end != null">
                and entrydate between #{begin} and #{end}
            </if>
        </where>
    </select>

    <!--根据username，和 passoword 登录-->
    <select id="selectByNamePassword" resultType="com.xrui.pojo.Emp">
        select *
        from emp
        where username = #{username}
          and password = #{password}
    </select>
</mapper>